# FE-001 — web shell increment

## Date
- 2026-03-14

## Summary
- Reworked the home page into a task-first search/results shell instead of a hero-first landing page.
- Made the list the primary decision surface and the map a supporting context with local selected-state syncing.
- Refined place detail hierarchy so verdict + trust summary appear before the full rule breakdown.
- Kept `unknown` distinct from provider `cache miss` with dedicated copy and routes.
- Added a dedicated Google provider-resolution route so live search can hit `GET /places/resolve/google/{googlePlaceId}` before entering canonical detail pages.
- Moved confidence to a quieter supporting label while surfacing source type + last checked near the verdict for non-unknown states.

## Files touched
- `apps/web/app/page.tsx`
- `apps/web/app/place/[placeSlug]/page.tsx`
- `apps/web/app/place/google/[googlePlaceId]/page.tsx`
- `apps/web/app/place/[placeSlug]/loading.tsx`
- `apps/web/app/not-found.tsx`
- `apps/web/components/search-shell.tsx`
- `apps/web/lib/api/adapters.ts`
- `apps/web/lib/place-repository.ts`
- `apps/web/lib/routes.ts`
- `apps/web/lib/types.ts`
- `apps/web/lib/view-models.ts`

## Verification
- `npm run typecheck` ✅
- `npm run build` ✅

## Notes
- UI still uses local mock data, but the structure now matches the intended API-backed UX more closely.
- Live API search results now route through provider resolution first, which allows the web app to render the real `PLACE_CACHE_MISS` state instead of skipping straight to canonical detail.
- Map interaction is intentionally lightweight for MVP: pins mirror the list and never silently filter results.
- Unknown detail now offers a venue website action when available.

## Suggested next step
- Wire the search shell to `GET /places/search` and `GET /places/{placeId}`, then replace the current mock map with real coordinates + pin selection.
