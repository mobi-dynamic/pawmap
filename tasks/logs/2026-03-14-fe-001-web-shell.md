# FE-001 — web shell increment

## Date
- 2026-03-14

## Summary
- Turned `apps/web` from package-only scaffold into a runnable minimal Next.js App Router shell.
- Added a search/home page shell, place detail shell, loading UI, not-found UI, and explicit cache-miss + unknown state handling.
- Kept all UI contracts app-level via local mock types and mock data.

## Notes
- UI intentionally avoids provider-native shapes even though backend contracts currently mention `googlePlaceId`.
- Report submission UI is deferred because MVP reports require auth and this slice is focused on public read flows.

## Suggested next step
- Wire the search shell to `GET /places/search` and replace mock state with API-backed view models.
